
__pycache__/
.env
.env.*
.venv/
*.log
*.pyc
*refresh_token*
# --- Phase 3 additions: append these to the repo's existing .gitignore ---
# Local credential files. Secrets belong in GitHub Actions secrets, nowhere else.
# Python
# Run artefacts — regenerated every run, never committed
client_secret*.json
out/
